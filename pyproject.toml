[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "prem-valuation"
version = "0.1.0"
description = "Premier League player market-value modelling"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
